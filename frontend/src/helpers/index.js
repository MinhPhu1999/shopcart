import { totalItems, totalPriceOfItems } from './subTotalCartItems';
import { generateOTP } from './genOTP';
import history from './history';

export { totalItems, totalPriceOfItems, generateOTP, history };
